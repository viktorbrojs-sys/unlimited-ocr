---
name: zcode-desktop-ru-localization-deferred
description: Пользователь хочет русский UI ZCode Desktop; исследование
  проведено, задача отложена до «станет не комфортно»
metadata:
  node_type: memory
  type: project
  originSessionId: sess_0a3c3bc7-a4da-4e36-a726-44f5d483d5aa
---

Пользователь (2026-08-16) интересуется русской локализацией интерфейса ZCode Desktop App, но пока отложил задачу: пользуется английской версией, вернётся к вопросу, если станет некомфортно. Цель — именно оболочка приложения, не Chromium-диалоги.

Установленные факты (экономят повторную разведку):
- AppImage: `/home/srs/Applications/ZCode-3.7.7-linux-x64_5c318ab301f0924231ffd0fac22b3622.AppImage`, самописный Electron (не VS Code-форк), автoupdater на месте (патч стирается при обновлении).
- squashfs со смещения **188392** байта (gzip, `unsquashfs -o 188392`), UI — в `resources/app.asar` (296 МБ, минифицирован).
- В `locales/` только `en-US.pak` и `zh-CN.pak` → у вендора есть внутренняя i18n en/zh, вероятно словари внутри asar (не проверено — asar не распаковывали).
- Система пользователя `ru_RU.UTF-8`; дешёвая полумера — подложить готовый `ru.pak` от Electron той же мажорной версии (версию Electron можно прочитать из `LICENSE.electron.txt` после распаковки). Переведёт только Chromium-диалоги, не оболочку. Пользователю эта полумера не нужна — он показал это явно.
- Конфиг приложения: `~/.config/ZCode` (без language packs), данные: `~/.zcode/`.

Согласованный план на будущее: не трогать оригинал; распаковать asar (`npx @electron/asar`), найти i18n-словари → добавить ru-RU как копию en-US с переводом и переключить locale; иначе — точечная замена строк в минифицированных чанках. Собрать отдельный `ZCode-ru-...AppImage` + скрипт `patch-zcode-ru.sh` для повторного наката после обновлений.
